{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run Docker Compose",
            "type": "shell",
            "command": "sh ./deploy.sh",
            "group": {
              "kind": "build",
              "isDefault": true
            },
            "problemMatcher": [],
            "detail": "This task builds the Docker image, pulls the latest version, and restarts the container using Docker Compose."
        }
    ]
}